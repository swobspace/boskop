<% content_for :head do %>
  <meta name="turbolinks-cache-control" content="no-cache">
<% end %>

<h1><%= t('controller.lines') %></h1>

<%= render 'index', lines: @lines %>
<br />

<%= back_link %>
<%= new_link [@framwork_contract, Line] %>
<%= link_to t('actions.reset_table'), "#", id: "reset_table", class: "btn btn-secondary" %>
