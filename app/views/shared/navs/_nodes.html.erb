<% if can? :navigate, :org_units %>
  <li class="nav-item dropdown <%= orgunits_active_class %>">
    <a class="nav-link dropdown-toggle" href="#" id="navbarOrgUnitsLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      <%= t('boskop.locations') %>
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarOrgUnitsLink">
      <%=link_to raw('<i class="fas fa-fw fa-table"></i> '+t('boskop.locations')), main_app.locations_path, class: 'dropdown-item' %>
      <% if can? :create, Location %>
        <%=link_to raw('<i class="fas fa-fw fa-plus"></i> '+t('boskop.new_location')), main_app.new_location_path, class: 'dropdown-item' %>
      <% end %>

    </div>
  </li>
<% end %>
